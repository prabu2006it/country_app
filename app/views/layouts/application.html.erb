<!DOCTYPE html>
<html>
  <head>
    <title>CountryApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/selectize.js/0.12.6/js/standalone/selectize.min.js" integrity="sha256-+C0A5Ilqmu4QcSPxrlGpaZxJ04VjsRjKu+G82kl5UJk=" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/selectize.js/0.12.6/css/selectize.bootstrap3.min.css" integrity="sha256-ze/OEYGcFbPRmvCnrSeKbRTtjG4vGLHXgOqsyLFTRjg=" crossorigin="anonymous" />
    <%= javascript_include_tag 'bootstrap.min', 'data-turbolinks-track': 'reload' %>
    <style>
      html, body, div, span  {
        margin: 0;
        padding: 0;
        border: 0;
        font-size: 100%;
        font: inherit;
        vertical-align: baseline;
        outline: none;
        -webkit-font-smoothing: antialiased;
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
        -webkit-box-sizing: border-box;
        -moz-box-sizing: border-box;
        box-sizing: border-box;
      }
      html { overflow-y: scroll; }
      body { 
        font-size: 62.5%;
      }


      table { border-collapse: collapse; border-spacing: 0; }
      img { border: 0; max-width: 100%; }

      h1 { 
        font-family: 'Amarante', Tahoma, sans-serif;
        font-weight: bold;
        font-size: 3.6em;
        line-height: 1.7em;
        margin-bottom: 10px;
        text-align: center;
      }

      .head1, .head2
      {
          display:inline;
          font-size: 1.5em;
      }

      .head1 {
        float: left;
      }

      .head2
      {
        float: right;
      }

      .main-header {
        text-align:center;
        margin-bottom: 10px
      }

      /** page structure **/
      #wrapper {
        display: block;
        width: 100%;
        height: 100%;
        background: #fff;
        margin: 0 auto;
        padding: 10px 17px;
      }

      #countries {
        margin: 0 auto;
        font-size: 1.2em;
        margin-bottom: 15px;
        height: 100%;
        width: 100%;
      }


      #countries thead {
        cursor: pointer;
        background: #c9dff0;
      }
      #countries thead tr th { 
        font-weight: bold;
        padding: 12px 30px;
        padding-left: 42px;
      }
      #countries thead tr th span { 
        padding-right: 20px;
        background-repeat: no-repeat;
        background-position: 100% 100%;
      }

      #countries thead tr th.headerSortUp, #countries thead tr th.headerSortDown {
        background: #acc8dd;
      }

      #countries thead tr th.headerSortUp span {
        background-image: url('https://i.imgur.com/SP99ZPJ.png');
      }
      #countries thead tr th.headerSortDown span {
        background-image: url('https://i.imgur.com/RkA9MBo.png');
      }

      #countries tbody tr { 
        color: #555;
      }
      #countries tbody tr td {
        text-align: center;
        padding: 15px 10px;
      }
      #countries tbody tr td.lalign {
        text-align: left;
      }

      .fnt-1_5 {
        font-size: 1.5em;
      }

      .modal-header {
        background-color: #33b5e5;
      }
    </style>    
  </head>
  <% flash.each do |type, message| %>
    <div class="alert flash-header fixed-alert <%= bootstrap_class_for(type) %>">
      <h4>
        <%= message %>
        <button class="close" data-dismiss="alert">×</button>
      </h4>
    </div>
<% end %>

  <body>
    <%= yield %>
  </body>
</html>
